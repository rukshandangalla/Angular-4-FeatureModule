import { VehicleSortingPipe } from './vehicle-sorting.pipe';

describe('VehicleSortingPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleSortingPipe();
    expect(pipe).toBeTruthy();
  });
});
