import { VehicleSelectionDirective } from './vehicle-selection.directive';

describe('VehicleSelectionDirective', () => {
  it('should create an instance', () => {
    const directive = new VehicleSelectionDirective();
    expect(directive).toBeTruthy();
  });
});
